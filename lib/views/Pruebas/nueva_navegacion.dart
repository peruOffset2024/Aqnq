import 'package:flutter/material.dart';

class NewNavigator extends StatelessWidget {
  const NewNavigator({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
        body: Center(child: Text('Aprendiendo de ubicaciones')));
  }
}
